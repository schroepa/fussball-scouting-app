# @vite-pwa/astro deklariert offiziell nur Astro <=5 als Peer-Dependency,
# funktioniert aber nachweislich auch mit Astro 7 (siehe astro.config.mjs).
# Ohne dieses Flag schlägt `npm install` mit einem ERESOLVE-Fehler fehl.
legacy-peer-deps=true
